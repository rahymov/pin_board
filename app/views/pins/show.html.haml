= image_tag @pin.image.url(:medium)
%h1= @pin.title
%p= @pin.description
%p Submitted by 
= @pin.user.email

%br/

= link_to "Back", root_path, class: "btn btn-default"
= link_to "Edit", edit_pin_path, class: "btn btn-default"
= link_to "Delete", pin_path, class: "btn btn-danger", method: :delete, data: {confirm: "Are you sure?"}